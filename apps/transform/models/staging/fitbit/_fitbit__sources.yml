version: 2

# See toggl_track sources.yml for the source → wrapper → stg lineage rule.

sources:
  - name: raw_fitbit
    description: "Fitbit raw API responses (append-only base tables)"
    schema: data_warehouse_v2
    tables:
      - name: raw_fitbit__sleep
        description: "Fitbit Sleep API v1.2 — full revision history"
      - name: raw_fitbit__activity
        description: "Fitbit Activity Daily Summary API v1 — full revision history"
      - name: raw_fitbit__heart_rate
        description: "Fitbit Heart Rate API v1 — full revision history"
      - name: raw_fitbit__hrv
        description: "Fitbit HRV API v1 — full revision history"
      - name: raw_fitbit__spo2
        description: "Fitbit SpO2 API v1 — full revision history"
      - name: raw_fitbit__breathing_rate
        description: "Fitbit Breathing Rate API v1 — full revision history"
      - name: raw_fitbit__cardio_score
        description: "Fitbit Cardio Score (VO2 Max) API v1 — full revision history"
      - name: raw_fitbit__temperature_skin
        description: "Fitbit Temperature Skin API v1 — full revision history"
