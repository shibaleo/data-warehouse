version: 2

sources:
  - name: raw_zaim
    description: "Zaim API raw responses (append-only base tables)"
    schema: data_warehouse_v2
    tables:
      - name: raw_zaim__money
        description: "Money records (income / payment / transfer) — full revision history"
      - name: raw_zaim__category
        description: "User categories — full revision history"
      - name: raw_zaim__genre
        description: "User genres (subcategories) — full revision history"
      - name: raw_zaim__account
        description: "User accounts (wallet / bank / card / e-money) — full revision history"
