name: 'dwh'
version: '1.0.0'
config-version: 2

profile: 'dwh'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

vars:
  timezone: "Asia/Tokyo"

models:
  dwh:
    staging:
      +materialized: view
    dimensions:
      +materialized: view
    facts:
      +materialized: view
